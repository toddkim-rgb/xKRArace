# parser/ingest.py REST 모드 설정.
# 이 파일을 parser/.env 로 복사한 뒤 값을 채우세요. (.env 는 git 에 올라가지 않습니다)
#
# SUPABASE_SERVICE_ROLE_KEY 는 Dashboard → Settings → API → "service_role" 키입니다.
# RLS 를 우회하는 강력한 비공개 키이므로 절대 커밋/공유하지 마세요.
# 키 없이 적재하려면:  python parser/ingest.py <폴더> --sql  → 생성된 SQL 을 SQL Editor 에 실행

SUPABASE_URL=https://nleeacvlyirlbiqkgqoa.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
